apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mariadb
spec:
  volumeClaimTemplates:
    - metadata:
        name: mariadb-data
      spec:
        storageClassName: ssd
        resources:
          requests:
            storage: 40Gi
